spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/linguaai
    username: lingua_user
    password: lingua_pass
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect