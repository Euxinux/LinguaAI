spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/linguaai
    username: lingua_user
    password: lingua_pass
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect